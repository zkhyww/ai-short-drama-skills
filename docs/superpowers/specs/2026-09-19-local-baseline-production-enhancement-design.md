# 本机基线、制作能力补强与 main 整合设计

日期：2026-09-19。状态：用户已确认设计；实施计划待审，未开始产品实现、安装同步或发布。

## 1. 已确认的目标和边界

用户已确认：**以本机最新版为基线，保留现有改动，再补；补完之后再同步合并最新的 main。**

本次吸收 manju-laoli 的有效增量：最终提示词的确定性检查、动作与法术的具体表现方案、按需只读制作看板，以及空间选路、亲缘外观、表演执行中的真实缺口。继续采用原规则位覆盖、合并或替换，不在 Skill 尾部追加补丁，不重建团队、正典、评分体系或案例索引。

这是包含新增工具和版本整合的架构级扩展，遵循 Superpowers 的设计审阅、实施计划审阅、测试与审查流程。基线及功能范围已获批准，不代表尚未编写的实现计划已获批准。

本次不付费生成媒体，不安装外部 Skill，不修改用户剧本，不把创作样本放进 Skill，不清理其他任务文件。同步合并在补充和验证完成之后进行，设计阶段不提前推送。

## 2. 基线与现状证据

| 对象 | 当前核实值 | 用途 |
|---|---|---|
| 本机 Drama Crew | 6.26.12 | 创作规则及交接行为基线 |
| 本机 Drama Studio | 1.21.8 | 制作规则及工具行为基线 |
| 设计前仓库 main | `29ee0e76ad151183de6161aef6e789c9887ce6be` | 开工源码快照，不替代本机新版 |
| 该 main 快照所含版本 | Crew 6.23.3 / Studio 1.17.3 | 与本机存在实质分叉，不能整包覆盖安装版 |
| 既有未提交文件 | `drama-studio/references/case-library/README.md`、`metadata.json` | 保持原样，不混入本次提交 |

本机两个入口文件的 SHA-256：

- Crew：`0A11FA7C810DCAE760098AEE37B35374BCAD2BF58DB5D6090CF6C5EA33937E23`
- Studio：`8D8A766100B272E5249AC4C4A8AE4D6D9C3A4A5F78095B02949DB15C3EABB550`

这些哈希只固定本次设计时的入口快照，不代替施工前的完整文件核验。施工时若安装版或工作区已被其他任务更新，先核新差异，再确定基线，不覆盖并发修改。

已核实的差异不是版本号变化：本机具有 `drama-crew/references/method-routing.md`、Studio 的 `emotion-performance-library.md` 与 `blender-previs.md`，当前 main 中没有这些文件。源码整合必须保留这些已存在能力及其必要依赖。

外部参考固定为 [manju-laoli V6.8 快照](https://github.com/lixiaoxiao9888-create/manju-laoli-skill/tree/dfd429faeaa753d6594f6dada37733beaa9333f8)，只迁移适配后的机制。其七段式、固定拍数、15 秒旧限制和一概禁首尾帧的口径不进入本项目。需要实质复用外部代码或文字时保留 MIT 归属；优先独立实现检查器与看板。

## 3. 方案选择

| 方案 | 影响 | 选择 |
|---|---|---|
| 在本机既有主责位置补方法，新增两个按需只读工具，再整合 main | 保留已验证流程，新增能力有独立测试入口 | 采用 |
| 整体迁移外部七段式及分级、审批流程 | 需要改动现有格式、能力路由和用户习惯 | 不采用 |
| 仅增加人工自检说明 | 变更较小，但时间线、引用和复制一致性仍无程序检查 | 仅保留其语义审查部分，不作为完整方案 |

工具分为两个可分别验证的交付单元：提示词检查器与只读看板。看板不成为检查器的前置依赖；不打开看板也能完成原制作流程。实施计划分别安排两者，不并行改动共享规则。

## 4. 原规则位整改表

以下路径以本机正式包为内容基线，施工时映射到隔离工作区中的同名路径。

| 主责文件 | 原段落 | 已确认的薄弱点 | 原位处理 |
|---|---|---|---|
| Studio `references/prompt-assembly.md` | §2、§3、§7 装配自检 | 八段、声音单一正文、时间线等靠人工核；§7 仍有“通用【声音】/专项 Block 5”等旧格式称谓 | 保留 §2 为格式唯一主责；§7 接检查器及未核状态，清理被替代的格式表述；旧稿只按受影响 Clip 兼容处理 |
| Studio `scripts/dreamina_route.py` | `_require_prompt`、视频命令预览入口 | 正文只检查非空，没有八段内容检查 | 为 Studio 最终正文增加文件输入和检查路径，复用同一正文生成命令预览；保留普通字符串及图片模式兼容 |
| Studio `references/dimensions/dim-action.md` | ②选择表、③修复规则 | “打斗对招”主要是景别建议，具体应对与力量表现较少 | 原表补可选攻防、重击、追击、群体参与及终局方案；原修复表对应真实失败 |
| Studio `references/dimensions/dim-vfx.md` | ②选择表、③修复规则 | 攻击、变身等已有，但防御反制、范围效果、实体与派生关系缺具体选法 | 原表扩展适用条件、动作/效果关系与检查落点，不另造法术总控 |
| Crew `references/genre-and-fight-rules.md` | “动作戏判断”“魔法、变身与规则效果” | 已有目标、过程、结果边界；需要与新增表现方案的事实需求对齐 | 只补使攻防选择、规则效果和状态结果成立的故事事实，不把机位或工程字段塞入剧本 |
| Crew `references/writing-craft.md` | §12 动作及现有场面表达段 | 需防止下游方法有了、上游仍只写泛化标签 | 先验证当前条款是否已足够；有遗漏才原位补与题材规则的引用，不重复整张方法表 |
| Studio `references/dimensions/dim-blocking.md` | ②选择表、③左右颠倒/镜像站位 | 俯视图触发条件仍包含“≥2 人”，未覆盖单人复杂空间 | 按空间依赖和已有参考是否足够选路，不按人数判风险；已能说明空间的图继续复用 |
| Studio `references/asset-library.md` | §3 身份区分门 | 有身份区分，无亲缘共性与个体差异的明确处理 | 在现有锚点规则内补条件分支，不新增人物库或固定相似比例 |
| Studio `references/dimensions/dim-performance.md` | 情绪库入口、③修复规则、④时间轴翻译 | 对可见部位和嘴部动作与发声冲突的处理可更直接 | 补执行条件和修复方法；已有情绪条目不重建，确有易混缺口才改共用库对应原条目 |
| Studio `references/file-management.md`、`SKILL.md`、启动卡 | 既有交付、文件职责和功能入口 | 目前没有只读看板工具 | 将看板接在按需展示处，明确派生快照职责；不新增每项目必交报告 |

README、CHANGELOG、启动卡及直接消费端只同步实际变化。能力卡不为适配外部模板改写；两包共用启动卡继续一致。不能仅删除旧测试中的措辞断言而不补新行为覆盖。

## 5. 提示词检查器

### 5.1 输入、入口和输出

新增 Studio `scripts/validate_prompt.py`，使用 Python 标准库，只读检查，不改正文、不联网、不生成媒体。

检查对象是一份最终 Clip 提示词原文。可读取当前任务已有的总时长、锁定发声条目、实际参考清单及版本信息作为对照；不要求用户再维护一份独立真源。没有结构化对照时只运行有依据的检查，其他项明确记未核。

正式 Studio 视频流程在装配自检中自动调用。Dreamina 视频预览增加 `--prompt-file` 输入，与原 `--prompt` 互斥：文件入口用于 Studio 八段最终正文，检查通过后把同一原文传入现有预览。原 `--prompt` 保留普通提示词兼容，图片命令不接受视频八段检查。其他模型可单独使用检查器，不强制经过 Dreamina。

结果包含检查覆盖范围、错误、警告和未核项目，各项带稳定代码、原文位置及原因。终端默认简短输出，可按需输出机器可读结果；不默认保存额外报告。退出码 0 表示已执行检查未发现错误，仍须列明缺少对照而未核的项目；1 表示存在确定性错误；2 表示输入不可用或必需结构未能解析。仅通过部分检查不得写“整条提示词已验证”。

### 5.2 程序负责的确定性检查

- 八段标题、顺序、重复、遗漏和空内容。接受正式中文标题及已记录的英文等义标题；“无/NONE”按本段实际用途合法，不新增第九段。
- 顶层镜头时间线：起点、终点、倒序、空档、重叠和实际总时长一致；动作节点不能误算成新镜头。一镜到底的内部时间节点不自动视为剪辑。
- 声音 ID 的重复定义、未定义引用、声窗超出 Clip。相同台词被不同 ID 合法复述不因文字相同判错；明确合唱、抢话等允许并行，不套逐句顺序播放。
- 有锁定逐字发声对照时，比较角色、类型、顺序和原句；不擅自改拼写、语气词、数字或语言。没有对照只能核结构，不能认证“未改词”。
- 有实际参考清单时，比较引用与本次输入的对应关系；文件不存在、无对应项或明确引用错误才拦截。不把文字资产 ID 当媒体句柄，不把文件存在写成已上传或内容正确。
- 概述明确给出的镜数/总时长与可解析时间线一致。人物动机、道具生命周期、光向、风格和因果等语义判断仍留原装配自检及独立核查。

不能可靠解析的写法须指出具体未核位置，要求当前执行者补明确的结构或人工复核；不猜测后发“通过”。批量提示词文件只按可明确定位的 Clip 分隔；无法判定正文边界时不自动截取一段冒充最终正文。

### 5.3 不作为通用拦截条件

不设每镜固定时长、镜数或招数，不一概拦硬切、静止、首尾帧、30 秒、相同镜长或无对白；不以字符数推断审美、传播效果、真实口型或视频成功率。普通对白和演唱的估时分开，估算风险不是已观看证据。

格式语言与对白语言分别继承目标配置。默认标题口径与用户明确要求的英文 Prompt 均可成立，不能因中文标题改写英文台词。仅案例使用英文或某个时长，不改变目标项目设置。

## 6. 方法补强及 Crew → Studio 联动

### 6.1 动作与法术

将具体方法写入现有选择表，每行回答：什么问题适用、可选表现、需要哪些已定事实、结果在哪里可观察。保留一条主路径，其他方法只解决不同缺口，不要求每场齐套。

动作侧补清：闪避/格挡/卸力等不同应对、重击的接触与受力反馈、击退后的距离和路线、追击的阶段状态、群体焦点与各参与者持续位置、终局及余波。终局不自动增加死亡、伤损或胜负反转；无伤训练和轻触按既定强度处理。

法术侧补清：拦截、偏折或抵消的结果差异；束缚与解除；持续领域的边界和内外影响；法器实体与派生效果的数量、控制关系和去向；多阶段变化与收束。触发、作用范围、代价与结束条件以正典为准，不默认所有法术需要法器、消耗、五行或环境破坏。

Crew 正文写出目标、实际选择、必要回应及阶段结果，规则场还要让已批准的触发、作用和边界可理解。Studio 据此设计参考、调度、摄影、声音与效果。表现方法未写明时自主设计；承重事实缺失时仅退依赖项，不用案例补故事设定，不凭题材标签升级破坏规模。

### 6.2 空间、亲缘与表演

- 空间选路先看既有地标/Panel 是否足够，再判断是否需要补反向视域、俯视调度或白模预演。单人镜面、环形路线、穿越遮挡也可能高风险；多人普通对话也可能无需新图。预演仍遵守既有工具和授权条件。
- 亲缘视觉只在关系已确认且需要体现时使用：继承已锁共性，补足真实可辨的个体差异。不设相似百分比，不以外貌推断亲属，不要求情侣、师徒或同阵营长得像；揭示前避免视觉提前泄密。
- 表演先核当前景别和遮挡，再选可见反应；背影或遮脸不硬写可见瞳孔/嘴角。闭唇、饮用、咀嚼等与清晰说话发生执行冲突时调整时间或镜头，保留原对白；正常微笑着说话、哽咽发声等不被机械禁止。易混情绪先按刺激、对象与行为结果区分，不用 AU 或肌肉百分比诊断真实心理。

## 7. 按需只读制作看板

新增 Studio `scripts/build_production_board.py`，从本次明确指定的现有分镜、提示词和资产记录读取，生成一份本地静态 HTML；不建服务、数据库或自动扫描整盘，不新增可编辑的活动真源。

展示：Clip/镜头定位、最终提示词、实际资产引用、源文件/版本、生成时刻及当前记录中已有的检查状态。缺关系或无法识别的旧格式直接提供原文件链接，不编造关联。看板标题明确是生成时快照，源文件更新后重新生成，不声称实时同步或继续沿用旧 QC。

复制按钮只复制选定 Clip 的最终提示词原文，不带来源、分析、状态徽标或检查报告；无法明确正文边界时不提供“最终 Prompt”复制。复制失败有明确反馈和手动选择文本的后备方式。

所有外来文字作文本转义，路径和链接只接受任务范围内的已明确输入，拒绝脚本式链接和意外路径越界。不读取凭据，不自动下载或内嵌媒体、不上传云端；本地路径可用于本地看板，但看板及项目内容不随 Skill 发布。

看板仅在用户请求或当前交付确需集中浏览时生成；不打开它、不提供完整资产数据或只要一条提示词，都不影响原有制作功能。

## 8. 基线整理、安装同步和 main 合并

1. 实施阶段在隔离工作区固定本机内容快照，保留原安装版可恢复副本。迁入可发布的规则、必要依赖和脚本，单独形成基线提交；这部分是保存既有成果，不算本轮新发明。
2. 不复制 Cookie、凭据、私有绑定、缓存、原始案例全文、项目剧本、视频或其他本机资料。公开案例继续沿现有发布机制，两个已脏案例文件保持原样，不自动暂存、回退或顺手提交。
3. main 独有的脚本修复、测试和文档不得因本机文件覆盖而丢失。按共同来源、各侧改动和实际行为核对，记录必要的冲突处置；旧断言失效时保留检查目的，用新合同测试替代。
4. 补充各自提交并通过验证后，再获取远端最新 main，将其合入工作分支。冲突按已批准行为和真实能力裁决，不整包选“本机”或“main”。合并后重跑受影响测试及完整回归，再更新 main。
5. 安装同步前重新核本机文件哈希，只更新本任务已验证的对应文件；如其他任务已有新编辑，暂停该文件并重新合并。保留私有绑定与可恢复原版，验证安装版与拟发布版的应同步文件一致。
6. 版本、README、CHANGELOG、启动卡按实际结果同步。版本号按合并时已占用版本顺延，禁止回退或重用已发布号。远端同步前再次检查敏感内容、差异和验收证据，不以本地提交冒充已发布。

不对 main 强推，不用重置或清理命令处理冲突。若现有脏文件与实际合并范围相撞，保护原文件并报告具体冲突，不默认替用户提交。

## 9. 验证与验收

实施计划必须先安排失败测试，再做最小实现。以下均是待执行项目，本文不宣称测试已经通过。

| 验证层 | 必须覆盖的正反条件 | 能证明什么 |
|---|---|---|
| 检查器单元与命令集成 | 中文/英文八段、无对白/无文字、30 秒多镜、一镜到底、合法同镜长；缺段、倒序/空档/超时、声音悬空、锁词被改、引用不匹配；无对照及不可解析输入不假通过 | 确定性检查与兼容边界 |
| 接口回归 | 普通 `--prompt` 和图片调用仍可用；文件正文与预览正文一致；首尾帧能力仍由实际模型/模式裁决；测试不提交生成任务 | 新入口不破坏已有工具 |
| 只读看板 | 复制与原文一致、批量 Clip 边界、中文路径、缺文件/旧格式、过期快照说明、HTML/链接注入、路径边界、复制失败后备、无媒体打包 | 展示安全与不污染 Prompt |
| 无技法暗示的行为任务 | 现代无伤训练与超现实重击；单人镜面路线与普通多人对话；有亲缘/无亲缘；已定法术与缺机制；英文对白伴嘴部动作；明确/遮挡表演 | 主动判断、适配与实际文字落点 |
| 规则及安装一致性 | 原位主责与消费端可达、旧表述退役、启动卡一致、动态人物数与资产复用、私有绑定保留、合并前后回归 | 新旧规则和两包同步不丢功能 |

行为基线先使用未补强规则运行，再用修订规则复测，实际记录选择与产物。对本来已能正确处理的项不为了增加功能数继续加规则；文本方法塑形按 writing-skills 做有/无指导对照及重复样本，并人工核读。创作样本和行为证据不进入运行时 Skill。

必须区分：静态合同通过、程序测试通过、行为任务通过、真实媒体通过。本轮不生成媒体，所以不能据此宣布打击感、口型、表情或整条无人值守生产线已验收。

## 10. 当前状态与下一步

- [x] 用户确认以本机最新版为基线，补完再同步合并最新 main。
- [x] 只读核验本机版本、main 差异与原规则位置。
- [x] 形成这份设计，区分既有能力与本轮增量。
- [x] 用户审阅本设计，并回复“确认，请实施”。
- [ ] 编写实施计划，用户审阅并确认执行方式。
- [ ] 隔离工作区保全基线、测试先行补强、独立审查。
- [ ] 合并最新 main、完成安装核验及获准范围的远端同步。

设计已确认；按 [检查器与方法实施计划](../plans/2026-09-19-production-checks-and-methods.md) 和 [只读看板与整合实施计划](../plans/2026-09-19-readonly-board-and-main-integration.md) 继续。计划审阅完成前不修改正式 Skill 或产品脚本；本文件不是实施完成报告。
