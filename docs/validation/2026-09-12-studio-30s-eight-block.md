# Studio 1.16.0：30秒直出八段式验证

## 范围与结果

正式源冻结于 `322f5278ff4416111b47dbb40e614ffe4e18a44c`；本轮分支起点为 `75b1269`。只修订 Studio 的格式主责与直接消费者，Crew 保持 6.22.0，未修改剧本创作、案例私有绑定或媒体合成程序。

启用30秒直出专项时，一 Clip 一条八段正文，固定00:00–00:30；英文项目使用英文标题、正文、对白和匹配口音，中文项目同架构匹配中文计速与声音。单张四区总表与已有多参考均可使用；三个镜头只是起排，一镜到底不强拆。普通任务仍沿用四区块。规则、角色派单、声音与QC速查均使用同一口径。

独立任务审查、最终源分支审查及一次最终修复差异复审已完成；阻挡安装的规则冲突均已原位修正。未新增通用框架或另建团队。

## 验证证据

| 检查层 | 实际执行 | 结论及边界 |
|---|---|---|
| 程序回归 | 最终冻结源运行 `python -m unittest discover -s tests -q` | 116项实际执行通过，0跳过，7.282秒；不是创作质量评分 |
| 差异检查 | `git diff --check 75b1269..322f527` | 通过；本轮无Crew运行文件变更 |
| 旧版消费 | 同题5次独立上下文，读取当时5份运行文件全文 | 未输出专项八段；中文计速、缺图与普通任务分流已有能力，不宣传为本轮新增 |
| 首轮新版完整消费 | 同题5次独立上下文 | 八段与主要语言路由成立；5份均出现容量超限时延长/分单的旧回退，2份存在明确计数或预算错误，因此这一批不算全通过 |
| 修后声音聚焦消费 | `2551cda`，同一B/C输入，5次独立上下文，只交第五段和容量裁决 | 5份逐句计数、声窗/纯发声/停顿一致性及固定30秒溢出处理均经逐份核算通过；不是5份完整八段或成片回归 |
| 定向规划消费 | `2551cda`，中文多图一镜到底、英文3D三镜、缺图、平台语言与声音回传 | 目标约束成立；平台语言题额外提及八段，不据此推断普通Vidu任务应启用专项 |
| 最终QC消费 | `322f527`，专项与普通任务成对裁决 | 专项超容回剧情层且不擅拆/延长；普通任务可在能力与授权内重排时长并保留四区块 |

常规实现与消费使用 Sol；最终整体审查使用 Astra，修复差异复审使用 Sol。所有创作回答与逐份核对留私有QA，不进入正式Skill或公开案例库。

## 本机同步

已同步本轮实际改变的11份Studio运行文件，并保留每份原始备份及实际补丁。主控独立核对：11份正文在LF规范化后均与冻结源一致；安装入口除版本和来源提交外，定制frontmatter逐字保持；版本为1.16.0，来源指向上述冻结提交。案例私有配置哈希前后不变。安装树新增0、删除0、非目标文件变更0，逐文件写前/写后raw哈希及换行差异保存在私有回执。

安装独立评审结论为需求符合、质量Approved：11份备份哈希、40份实际补丁的目标范围与前后哈希均已核对，未发现额外安装变更。

## 证据边界与保留

用户提供的真人与3D成片已由用户完整验收音画和口型，本轮接受其为用户实测成功证据，不要求重复证明。它们与本轮规则消费测试分别记录；本轮未新生成视频，不宣称跨项目成片稳定性已被验证。

第一次修复的完整RED终端输出未持久化；具体命令、可确认的失败定位/尾部与GREEN记录已保留，缺失如实披露，不补造。最终修复与程序回归另有本次记录。

原片、截图、样本回答、备份和审查工件本地保留；不纳入本次公开提交。未执行远端推送、双剧续写或关机。

聚焦文档一致性核对已覆盖入口、模板、直接消费者和发布说明。收尾清理了本轮两份一次性安装辅助脚本，实际补丁与回退备份保留；三处可再生测试缓存继续保留，未作递归或批量清理。
