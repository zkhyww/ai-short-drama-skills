# 启动配置引导验证

范围：Crew 6.23.0、Studio 1.17.0 的接稿展示、配置继承与格式路由。不验证整剧创作质量或媒体生成效果，不修改案例正文。

## 回归方法与观察

- 基线 116 项自动测试通过。新增单团安装测试先因包内缺少指南失败，补齐后全套 117 项实际执行通过，0 跳过。
- 两个源包通过 WorkBuddy Skill 校验；修改文档的本地链接、围栏和 diff 空白检查通过。Codex 安装副本需适配 `metadata.version` 并另用宿主校验器验证，不以源包校验替代。
- 使用 `gpt-5.6-sol`，在五个独立上下文分别输入“启动创作团，我第一次用，想看看怎么开始”和“启动制作团，我第一次用，手头有剧本，想看看能帮我做哪些事”。旧版主要给问卷；首版新引导已展示完整三组导航，但五轮均漏开场快捷入口。将快捷语纳入首答结构后，再做五轮独立复测，十份首答均给出完整导航、自动配置及“跳过引导／展开全部功能”提示。原始新答复留私有 QA，不打包进 Skill。
- 十个边界场景另覆盖：无目标只说跳过、局部台词、续作质检继承、英文单次 30 秒、中文项目借鉴英文参考、付费无预算、仅资产、展开不重置、整集时长与单次时长分离、单包快写。核对的是接稿范围与下一合法动作，未执行正文或媒体生产。
- 补测已锁 Kling 8 秒的中文续作、未选模型但明确单次 30 秒的英文规划；均保留目标约束。独立审查发现并修正残留的无条件 15 秒默认、跨宿主安装说明，以及“直接做”自动转裸本。修后另测“已审项目＋跳过引导，直接做下一镜”，保留 `reviewed`、原修订与模型设置，不重填、不产生消费。

## 可复核命令

```powershell
python -X utf8 -B -m unittest discover -s tests -q
python -X utf8 -B -m unittest tests.test_startup_guide_distribution -v
git diff --check
```

## 判定边界

已验证新手导航、快捷入口、来源分级配置与关键路由继承；这是启动交互和规则消费证据，不是“高质量自动成片”验收。发布仅含正式规则、文档、测试与此结果摘要；不含用户剧本、媒体、模型原始回答、账号或本机绑定。
