# 制作经验库（跨项目自进化 · 沈砚维护，改稿后按需加载）

> **解决的问题**：skill 本身是静态的，用户的改稿反馈（「太拖沓了」「名字太通用」「这个套路过时了」）过去只能靠手动改规则才能沉淀。本文件建立**「改稿 → 提取经验 → 升格为规则」**的闭环，让团队越用越贴合用户偏好。
> **借鉴来源**：drama-script-writing 的 S6 自我学习模块（改稿后自动对比初稿 vs 修改版）。去掉其「每步等确认」重交互部分，只留经验沉淀机制。

## 1. 触发时机（写死）

**用户对任何成员的产出做过修改/纠正后，沈砚必须主动执行本流程**——不等用户喊、不依赖 QC 是否通过。

**持久化纪律（v1.7.6 定版）**：普通改稿只在**当前任务内部**提炼经验候选，不落盘、不额外追问；只有用户明确说「记住 / 写入经验库 / 以后这样」或对提炼条目明确认可时，才写入本文件持久化；**写入后不自动删除、不自动压缩清退**（超体量时向用户提示并等裁决）。

| 用户行为 | 处理 |
|---|---|
| 用户手动改了产出，或说「这里改成XX」 | 改稿完成后在任务内提炼经验候选（不落盘不追问） |
| 用户说「写入」/「记下来」/「记住」 | 经验条目写入本文件，持久保留 |
| 用户说「**以后就这样**」/「**以后不要XXX**」/「**改规则**」 | **快速升格通道**：直接改写 references 正式规则 |
| 用户没表态 | 候选只在任务内有效，随任务结束丢弃；不落盘、不暂存、不倒计时 |

## 2. 提取流程

```
1. Read 本文件摘要（避免重复记录）
2. 索取/比对修改版：用户改过哪部分就收哪部分的最终版
3. 逐段对比：初稿 vs 修改版，标出差异
4. 提炼通用规律（不是个例）——「这次的第3集卡点弱」→「卡点必须带现实代价」
5. 用户已授权（说「记住/写入/记下来」或明确认可条目）→ **直接写入本文件，不再二次确认**；用户未授权 → 条目只在任务内有效，随任务结束丢弃
6. 超体量不打断创作，只在用户主动维护经验库时提示并等裁决
```

## 3. 经验条目格式

```
[YYYY-MM-DD] [维度]
- 观察：初稿与修改版之间发现了什么差异
- 规律：差异背后的通用规律（不是个例）
- 行动：下次写作/核查时应该怎么做
- 置信度：高(验证3次+) / 中(1-2次) / 低(仅1次)
```

## 4. 写入规则（5 条）

1. **只记通用规律**，不记具体案例（案例留在项目成稿里）
2. **只记 AI 常犯的错误与用户偏好**，不记显而易见的常识
3. **与现有规则重复的不记**（写前先 Read 对应 references 核对）
4. **与核心规则冲突的**标记「需讨论」，不直接写入
5. **写入以用户授权为前提**——用户明确说「记住/写入/记下来」或明确认可条目时直接写入不再二次确认；用户未授权的候选不落盘

## 5. 项目复盘仪式（项目交付后沈砚必跑 · v1.12.0 增，与 crew learnings §5 同构）

项目交付后，沈砚收尾制作端经验：汇总本项目的 QC 报告模式、失败图谱新条目、装配自检新命中、抽卡预算实耗 → 逐条判定去向（升格进 references / 留本库 / 丢弃）→ 跑合同测试全绿 → 升 patch → 提交推送。剧本侧经验不收（回传 crew learnings，见 §5 对照表末行）。

**单人 vs 团队分流（团队为默认，与 crew 同口径）**：默认走 **PR**——条目带证据，CI（`.github/workflows/contract-tests.yml`）机器先拦，人审价值，squash 合并；仅当仓库单作者且用户明示单人使用时走直推 main 快道。团队模式下不绕过 PR 直推 main。

## 6. 升格目标对照表（快速通道用）

| 经验维度 | 升格到哪个文件 |
|---|---|
| 分镜质量/微节拍/表情/防错 | `references/storyboard-craft.md` |
| 资产/角色锚定/场景板/九宫格 | `references/asset-library.md` |
| 镜头契约/六问/镜头卡/重拍 | `references/shot-contract.md` |
| 装配/七段式/映射表/平台适配 | `references/prompt-assembly.md` |
| 景别/运镜/光影/站位/表演/动作/风格/音效某维度 | `references/dimensions/` 对应维度卡 |
| QC 尺度/失败图谱条目 | `references/failure-atlas.md` |
| 剧本侧问题（节奏/选题/命名等） | 回传 drama-crew 的 learnings.md，不写进本库 |

## 7. 体量维护

- **触发**：本文件超过 5KB 或条目超过 30 条
- **动作**：**先向用户提示并等裁决**，不自动删除任何条目；用户同意后高置信度经验可「毕业」升格进 references 正式规则（升格不等于删除原条目，是否清理由用户定）；压缩后在变更记录留痕（日期/处理了几条/保留哪些/原因）

---

## 高置信度经验（已验证多次）

<!-- 随使用积累 -->

---

## 中置信度经验（验证 1-2 次）

<!-- 随使用积累 -->

---

## 低置信度经验（仅 1 次，仅作参考不自动清理）

<!-- 随使用积累 -->

---

## 需讨论（与现有规则冲突，暂存不执行）

<!-- 冲突经验先记这里，等用户裁决 -->

---

## 变更记录
- v4.5.0 建立本文件（经验库自进化闭环，drama-crew）
- v1.0.0 随拆分迁入 drama-studio 并改制作侧口径（维护者改由制作侧主理人沈砚担任；升格目标表换成制作侧文件；剧本侧经验回传 drama-crew）
