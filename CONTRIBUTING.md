# 贡献指南

感谢参与维护。这里的重点不是继续堆规则，而是让两个 Skill 在真实任务中更稳定。

## 提交前

1. 先确认问题能在当前 `main` 复现；
2. 找到主责文件和所有消费端，避免只修一处；
3. 优先最小修改，不新增角色、弹窗、文件或评分维度，除非问题确实需要；
4. 不提交项目剧本、媒体素材、模型凭据、个人路径或内部审计内容。

## Pull Request 应包含

- 问题及最小复现；
- 修改的合同和影响范围；
- 运行过的 validator 或行为测试；
- 版本号是否需要调整及理由。

涉及外部模型能力时，请给出 provider、adapter、版本、核实日期和一手来源。不要把某个接入方的参数写成所有供应商都适用的通用规则。

## 提交信息

没有特殊约定时使用：

```text
feat: add ...
fix: correct ...
docs: explain ...
test: cover ...
```

一个提交只做一件可审查、可回退的事。
